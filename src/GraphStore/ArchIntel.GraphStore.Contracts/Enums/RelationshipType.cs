namespace ArchIntel.GraphStore.Contracts.Enums;

public enum RelationshipType
{
    References,
    Calls,
    Implements,
    Inherits,
    Injects,
    Uses,
    Publishes,
    Consumes,
    Owns,
    Contains,
}
